import { createStore } from "vuex";
import models from "./modules/models";


export default new createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        models,
    }
});